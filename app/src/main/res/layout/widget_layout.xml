<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="8dp">

    <!-- Иконка приложения -->
    <ImageView
        android:id="@+id/app_icon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginEnd="8dp"
        android:src="@mipmap/ic_launcher" /> <!-- Используем стандартную иконку запуска -->

    <!-- Город -->
    <TextView
        android:id="@+id/widget_city_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/app_icon"
        android:layout_alignParentTop="true"
        android:text="Загрузка..."
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:textStyle="bold"
        /> <!-- Увеличен размер текста для города -->

    <!-- Температура -->
    <TextView
        android:id="@+id/widget_temperature"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_city_name"
        android:text="--°C"
        android:textColor="@android:color/white"
        android:textSize="48sp"
        android:textStyle="bold"
        android:layout_alignParentStart="true"
        android:layout_marginTop="4dp" /> <!-- ЗНАЧИТЕЛЬНО УВЕЛИЧЕН РАЗМЕР ТЕМПЕРАТУРЫ, Небольшой отступ сверху -->

    <!-- Условие погоды и эмодзи -->
    <LinearLayout
        android:id="@+id/condition_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_temperature"
        android:layout_alignParentStart="true"
        android:orientation="vertical"
        android:gravity="center_vertical"
        android:layout_marginTop="4dp"> <!-- Небольшой отступ сверху -->

        <TextView
            android:id="@+id/widget_condition"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="-"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            /> <!-- Увеличен размер текста для условия -->

        <TextView
            android:id="@+id/widget_condition_emoji"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="❓"
            android:textColor="@android:color/white"
            android:textSize="32sp"
            android:layout_marginTop="-4dp" />
    </LinearLayout> <!-- УВЕЛИЧЕН РАЗМЕР ЭМОДЗИ, небольшой отрицательный отступ для компактности -->

    <!-- Время обновления -->
    <TextView
        android:id="@+id/widget_last_updated"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:text="Обновлено: --:--"
        android:textColor="#CCCCCC"
        android:textSize="12sp" /> <!-- Немного увеличен размер времени обновления -->

</RelativeLayout>
